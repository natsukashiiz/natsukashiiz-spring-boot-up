spring:
  main:
    allow-circular-references: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/blog_test?useUnicode=yes&characterEncoding=UTF-8
    username: root
    password: YmIUGQw6KeJmIPnG??&xh!
  jpa:
    show-sql: false
    generate-ddl: true
    hibernate:
      ddl-auto: update
server:
  port: 920
ii:
  jwt:
    issuer: natsukashiiz
    expirationMs: 3600000
    private-key: classpath:certs/private.pem
    public-key: classpath:certs/public.pem